{
	"private": true,
	"devDependencies": {
		"lerna": "^2.11.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"test": "lerna exec --parallel -- yarn test",
		"nodemon":"lerna exec --parallel -- yarn nodemon"
	}
}
