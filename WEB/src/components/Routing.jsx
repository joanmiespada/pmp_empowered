import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import App from './App';

const Routing = () => (
  <BrowserRouter>
    <App />
  </BrowserRouter>
);

export default Routing;
